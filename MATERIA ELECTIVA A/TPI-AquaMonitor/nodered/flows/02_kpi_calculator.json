[
  {
    "id": "f2b22222-0001-4000-8000-000000000001",
    "type": "tab",
    "label": "02 - Calculo de KPIs",
    "disabled": false,
    "info": "Cada 60 s consulta agregados de la ultima hora en QuestDB, calcula los 6\nKPIs (2 por equipo) y los inserta en la tabla kpi_values via ILP.",
    "env": []
  },
  {
    "id": "n2b22222-0002-4000-8000-000000000002",
    "type": "inject",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "cada 60 s",
    "props": [{ "p": "payload" }],
    "repeat": "60",
    "crontab": "",
    "once": true,
    "onceDelay": "5",
    "topic": "",
    "payload": "",
    "payloadType": "date",
    "x": 130,
    "y": 100,
    "wires": [["n2b22222-0003-4000-8000-000000000003"]]
  },
  {
    "id": "n2b22222-0003-4000-8000-000000000003",
    "type": "function",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "armar query agregados",
    "func": "// Agregados de la ultima hora, una sola fila, formato largo -> condicional.\nconst sql = `SELECT\n  avg(CASE WHEN equipo='EQ01' AND variable='CAUDAL' THEN value END) AS caudal_eq01,\n  avg(CASE WHEN equipo='EQ01' AND variable='CORRIENTE' THEN value END) AS corriente_eq01,\n  avg(CASE WHEN equipo='EQ01' AND variable='ESTADO' THEN value END) AS estado_eq01,\n  avg(CASE WHEN equipo='EQ02' AND variable='TURB_IN' THEN value END) AS turbin_eq02,\n  avg(CASE WHEN equipo='EQ02' AND variable='TURB_OUT' THEN value END) AS turbout_eq02,\n  avg(CASE WHEN equipo='EQ02' AND variable='PRES_DIF' THEN value END) AS presdif_eq02,\n  stddev(CASE WHEN equipo='EQ03' AND variable='PH' THEN value END) AS ph_sd_eq03,\n  count(CASE WHEN equipo='EQ03' AND variable='PH' THEN 1 END) AS ph_n_eq03,\n  count(CASE WHEN equipo='EQ03' AND variable='PH' AND (value < 6.5 OR value > 8.0) THEN 1 END) AS ph_fuera_eq03\nFROM raw_variables\nWHERE ts >= dateadd('h', -1, now()) AND quality = 2`;\n\nmsg.method = 'GET';\nmsg.url = (env.get('QUESTDB_HTTP_URL') || 'http://localhost:9000') + '/exec?query=' + encodeURIComponent(sql);\nmsg.headers = {};\nconst token = env.get('QUESTDB_TOKEN');\nif (token) msg.headers['Authorization'] = 'Bearer ' + token;\nreturn msg;\n",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 340,
    "y": 100,
    "wires": [["n2b22222-0004-4000-8000-000000000004"]]
  },
  {
    "id": "n2b22222-0004-4000-8000-000000000004",
    "type": "http request",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "GET QuestDB /exec",
    "method": "use",
    "ret": "obj",
    "paytoqs": "ignore",
    "url": "",
    "tls": "",
    "persist": true,
    "proxy": "",
    "authType": "",
    "senderr": false,
    "headers": [],
    "x": 570,
    "y": 100,
    "wires": [["n2b22222-0005-4000-8000-000000000005"]]
  },
  {
    "id": "n2b22222-0005-4000-8000-000000000005",
    "type": "function",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "calcular 6 KPIs -> ILP",
    "func": "const r = msg.payload;\nif (!r || !r.dataset || !r.dataset.length) {\n    node.warn('Respuesta sin datos de QuestDB');\n    return null;\n}\nconst cols = r.columns.map(c => c.name);\nconst row = r.dataset[0];\nconst g = {};\ncols.forEach((c, i) => { g[c] = row[i]; });\n\nfunction num(v) { return (v === null || v === undefined) ? NaN : Number(v); }\n\nconst kpis = [];\nfunction push(equipo, kpi_name, value, unit) {\n    if (value === null || value === undefined || Number.isNaN(value)) return;\n    kpis.push({ equipo, kpi_name, value: Number(value), period: 'hour', unit });\n}\n\n// --- EQ01 ---------------------------------------------------------------\nconst caudal = num(g.caudal_eq01), corriente = num(g.corriente_eq01);\nif (corriente > 0) push('EQ01', 'eficiencia_energetica', +(caudal / corriente).toFixed(4), 'm3h_per_A');\npush('EQ01', 'disponibilidad_bomba', +(num(g.estado_eq01) * 100).toFixed(2), '%');\n\n// --- EQ02 ---------------------------------------------------------------\nconst tin = num(g.turbin_eq02), tout = num(g.turbout_eq02);\nif (tin > 0) push('EQ02', 'eficiencia_filtracion', +(((tin - tout) / tin) * 100).toFixed(3), '%');\npush('EQ02', 'presion_diferencial_media', +num(g.presdif_eq02).toFixed(2), 'mbar');\n\n// --- EQ03 ---------------------------------------------------------------\nconst phn = num(g.ph_n_eq03), phf = num(g.ph_fuera_eq03);\nif (phn > 0) push('EQ03', 'tiempo_fuera_rango_ph', +((60.0 * phf) / phn).toFixed(2), 'min');\npush('EQ03', 'desviacion_ph', +num(g.ph_sd_eq03).toFixed(4), 'pH');\n\nif (!kpis.length) { node.warn('No se pudo calcular ningun KPI'); return null; }\n\n// --- Formatear como lineas ILP para kpi_values ------------------------\nconst tsMs = Date.now();\nfunction esc(v) { return String(v).replace(/([,= ])/g, '\\\\$1'); }\nconst lineas = kpis.map(k =>\n    'kpi_values,equipo=' + esc(k.equipo) +\n    ',kpi_name=' + esc(k.kpi_name) +\n    ',period=' + esc(k.period) +\n    ',unit=' + esc(k.unit) +\n    ' value=' + k.value + ' ' + tsMs\n);\n\nmsg.payload = lineas.join('\\n');\nmsg.method = 'POST';\nmsg.headers = { 'Content-Type': 'text/plain; charset=utf-8' };\nconst token = env.get('QUESTDB_TOKEN');\nif (token) msg.headers['Authorization'] = 'Bearer ' + token;\nmsg.url = (env.get('QUESTDB_HTTP_URL') || 'http://localhost:9000') + '/write?precision=ms';\nnode.status({ text: kpis.length + ' KPIs @ ' + new Date(tsMs).toISOString() });\nreturn msg;\n",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 800,
    "y": 100,
    "wires": [["n2b22222-0006-4000-8000-000000000006"]]
  },
  {
    "id": "n2b22222-0006-4000-8000-000000000006",
    "type": "http request",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "POST kpi_values",
    "method": "use",
    "ret": "txt",
    "paytoqs": "ignore",
    "url": "",
    "tls": "",
    "persist": true,
    "proxy": "",
    "authType": "",
    "senderr": false,
    "headers": [],
    "x": 1030,
    "y": 100,
    "wires": [["n2b22222-0007-4000-8000-000000000007"]]
  },
  {
    "id": "n2b22222-0007-4000-8000-000000000007",
    "type": "debug",
    "z": "f2b22222-0001-4000-8000-000000000001",
    "name": "resultado insert KPIs",
    "active": true,
    "tosidebar": true,
    "console": false,
    "tostatus": true,
    "complete": "statusCode",
    "statusVal": "statusCode",
    "statusType": "msg",
    "x": 1050,
    "y": 180,
    "wires": []
  }
]
